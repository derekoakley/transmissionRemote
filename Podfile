# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'transmitRemote' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for transmitRemote
   pod 'DeepDiff', '~> 1.2'

  target 'transmitRemoteTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'transmitRemoteUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
